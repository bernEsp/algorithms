#### What?

Write a function fib() that a takes an integer n and 
returns the nth fibonaacci number.

#### how?

Let's say our fibonacci series is 0-indexed and starts with 0. SO
```ruby
fib(0) # => 0
fib(1) # => 1
fib(2) # => 1
fib(3) # => 2
fib(4) # => 3
fib(5) # => 5
fib(8) # => 21
fib(10) # => 55
fib(20) # => 6765
...


